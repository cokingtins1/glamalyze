import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

type Props = {
    
};

export default function SharedComparison({}: Props) {
	return (
		<Card>
			<CardHeader>
				<CardTitle>Shared Comparison</CardTitle>
			</CardHeader>
			<CardContent>comparison...</CardContent>
		</Card>
	);
}
